<div class="container-lg">
    <div class="row justify-content-around">
        <div class="col-sm-8 ms-2">
            <img src="@Model.Img" class="product-picture" />
            <p class="product-desc">
               @Model.LongDesc
            </p>
        </div>
        <div class="col-sm-3 d-flex flex-column align-items-center info">
            <div class="h4">
                @Model.Name
            </div>
            <div class="h5">
                @Model.Price.ToString("c")
            </div>
            <a data-ajax="true" data-ajax-method="post" class="to-cart btn btn-warning" id="tocart" asp-action="AddToCart" asp-controller="Cart" asp-route-id="@Model.Id">
                В корзину
            </a>
            <br />
            @if (Static.IAdmin)
            {
             <a asp-controller="Product" asp-action="Edit" asp-route-productId="@Model.Id">
                  Изменить
             </a><br />
             <a asp-controller="Product" asp-action="Delete" asp-route-productId="@Model.Id">
                 Удалить
             </a>}
             </div>
    </div>
</div>
@section Scripts{ 
}
